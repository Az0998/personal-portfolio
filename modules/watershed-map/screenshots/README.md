# Screenshots

放作品集封面 / 交互截图于此（建议 WebP 或 PNG，单张 &lt; 400 KB）。

建议至少三张：

1. `01-overview.png` — 首屏：底图 + 侧栏图层/统计  
2. `02-measure-threshold.png` — 测距虚线 + 阈值配色站点  
3. `03-series-drawer.png` — 过程线抽屉  

本地截图（开发服已启动时）：

```bash
# 浏览器打开 http://localhost:3000/watershed-map 后手动截图保存到本目录
```

可在作品 Markdown 中引用：`/../modules/watershed-map/screenshots/...` 不会被 Next 直接托管；若要上线展示，请复制到 `public/watershed-map/screenshots/`。
